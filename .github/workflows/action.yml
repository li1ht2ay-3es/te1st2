on:
#  push:
  workflow_dispatch:


env:
  pa5: ${{ secrets.pa5 }}


jobs:

  cron:
    runs-on: windows-latest

    steps:
      - uses: li5ht4ay-3es/ac5io4s@user-action


      - uses: li5ht4ay-3es/ac5io4s@checkout-branch
        with:
          token: ${{ env.pa5 }}


      - shell: cmd
        run: |
          # projectorrays.exe --dump-scripts --dump-chunks --dump-json project.cct project.1.cct
          projectorrays.exe --dump-scripts --dump-chunks --dump-json buzz1g.cct buzz1g.1.cct
          # projectorrays.exe --dump-scripts --dump-chunks --dump-json dirapi.cct dirapi.1.cct


      - uses: actions/upload-artifact@main
        with:
          path: |
            *.*
